module Questions

/**
 * Overall
 * Q: What does the default exactly do? (e.g. Basic::Factorial)
 * 	  (It can be removed, and it still works fine)
 * A: ???
 */
 
/**
 * Common::WordReplacement
 * Q: What does the i do in the function capAll2?
 * 	  (It can be removed, and it still works fine)
 * A: ???
 */