module Common::AdHocDataExploration

/**
 * Alex Kok (11353155)
 * alex.kok@student.uva.nl
 * 
 * Actualy read the page, but didn't run everything that was explained here.
 */
 
 public int fact(int n) {
 	if (n <= 1) {
 		return 1;
 	} else {
 		return n * fact (n-1);
 	}
 }
 
 